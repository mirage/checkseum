(copy_files#
 (alias install)
 (files ../src-c/native/*))

(rule
 (deps Makefile)
 (targets libcheckseum_freestanding_stubs.a)
 (action
  (no-infer
   (progn
    (run %{make})))))

(install
 (package checkseum)
 (section lib)
 (files libcheckseum_freestanding_stubs.a))
